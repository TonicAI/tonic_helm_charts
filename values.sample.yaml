# Declare variables to be passed into your templates.

replicaCount: 1

imagePullSecrets: []

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 443

resources: {}

autoscaling:
  enabled: false

tonicdb: {
  host: <db-host>,
  port: 5432,
  dbName: tonic,
  user: <user>,
  password: <password>,
  sslMode: Require
}

numberOfWorkers: 1

environmentName: <company-name>

enableLogCollection: false

tonicStatisticsSeed: <any-integer>

dockerRepoBaseUrl: quay.io/tonicai

nodeSelector: {}

tolerations: []

affinity: {}
